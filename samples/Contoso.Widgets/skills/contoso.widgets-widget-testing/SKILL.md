---
name: contoso.widgets-widget-testing
description: Testing patterns for code that uses Contoso.Widgets. Use when writing unit or integration tests involving widgets.
---

# Testing Contoso.Widgets

Use `WidgetFactory.CreateForTest()` to isolate tests from the production batching pipeline.
